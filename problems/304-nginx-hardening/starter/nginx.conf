# BROKEN: version disclosure, no security headers, no gzip, no proxy headers.
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name app.example.com;

        location / {
            proxy_pass http://app:8000;
        }
    }
}
